
export interface ContactItem {
  icon: string;
  title: string;
  content: string;
  link: string | null;
  linkText: string | null;
}

export interface SocialLink {
  name: string;
  icon: string;
  url: string;
}
